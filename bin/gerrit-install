#!/usr/bin/env bash
GERRIT_PATH=~/bin
GERRIT_BIN=$GERRIT_PATH/gerrit
SCRIPT_URL=https://raw.githubusercontent.com/jdlrobson/GerritCommandLine/master/gerrit.py
# if [ ! -e $GERRIT_BIN ]
# then
	echo 'Installing GerritCommandLine tool'
	curl -o $GERRIT_BIN $SCRIPT_URL
	chmod +x $GERRIT_BIN
# fi
